# Настройка Firebase Analytics

## ✅ Что уже сделано

1. ✅ Firebase SDK добавлен в проект через Swift Package Manager
2. ✅ `GoogleService-Info.plist` уже есть в проекте
3. ✅ Создан `AnalyticsService` для отслеживания событий
4. ✅ Интегрированы 3 события:
   - `first_open` - первый запуск приложения
   - `trial_start` - начало триала
   - `purchase` - успешная покупка

## 📋 Что нужно сделать в Firebase Console

### 1. Вставьте правильный GoogleService-Info.plist

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект (или создайте новый)
3. Перейдите в **Project Settings** (⚙️ → Project settings)
4. В разделе **Your apps** найдите iOS приложение или создайте новое
5. Скачайте `GoogleService-Info.plist`
6. Замените существующий файл `GoogleService-Info.plist` в корне проекта на новый

### 2. Включите Google Analytics

1. В Firebase Console перейдите в **Project Settings**
2. Прокрутите вниз до раздела **Google Analytics**
3. Если Analytics не включен:
   - Нажмите **Enable Google Analytics**
   - Выберите или создайте Analytics аккаунт
   - Нажмите **Enable**

### 3. Проверьте Bundle ID

Убедитесь, что Bundle ID в Firebase Console совпадает с Bundle ID вашего приложения:
- Текущий Bundle ID: `com.spectraghost.app`
- Проверьте в Firebase Console: **Project Settings** → **Your apps** → iOS app

## 📊 Отслеживаемые события

### 1. `first_open`
- **Когда**: При первом запуске приложения
- **Где**: `GhostApp.swift` → `init()`
- **Параметры**:
  - `timestamp` - время события

### 2. `trial_start`
- **Когда**: Когда пользователь начинает триал подписки
- **Где**: 
  - `SubscriptionViewModel.swift` → `purchaseSubscription()` (при обнаружении триала)
  - `PaywallView.swift` → `onDisappear` (при обнаружении активного триала)
- **Параметры**:
  - `timestamp` - время события
  - `product_id` - идентификатор продукта (если доступен)

### 3. `purchase`
- **Когда**: При успешной покупке подписки
- **Где**:
  - `SubscriptionViewModel.swift` → `purchaseSubscription()` (после успешной покупки)
  - `PaywallView.swift` → `onDisappear` (при обнаружении активной подписки)
- **Параметры**:
  - `timestamp` - время события
  - `product_id` - идентификатор продукта (если доступен)
  - `value` - цена покупки (если доступна)
  - `currency` - валюта (если доступна)

## 🧪 Тестирование

### Тестирование на Test Store (RevenueCat)

1. Запустите приложение в симуляторе или на устройстве
2. При первом запуске должно отправиться событие `first_open`
3. Пройдите онбординг
4. Попробуйте начать триал или совершить покупку через Test Store
5. События `trial_start` и `purchase` должны отправиться

### Просмотр событий в Firebase

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект
3. Перейдите в **Analytics** → **Events**
4. События появятся через несколько минут после отправки
5. Для тестирования можно использовать **DebugView**:
   - В Xcode добавьте аргумент запуска: `-FIRDebugEnabled`
   - Или используйте Firebase Console → **Analytics** → **DebugView**

## 🔍 Отладка

### Проверка инициализации Firebase

В консоли Xcode при запуске приложения должно появиться:
```
✅ Firebase configured successfully
```

### Проверка отправки событий

В консоли Xcode при отправке событий должно появиться:
```
📊 Analytics: first_open logged
📊 Analytics: trial_start logged with product_id: ...
📊 Analytics: purchase logged with product_id: ...
```

### Если события не отправляются

1. **Проверьте GoogleService-Info.plist**:
   - Убедитесь, что файл добавлен в проект
   - Проверьте, что Bundle ID совпадает
   - Убедитесь, что файл добавлен в Target Membership

2. **Проверьте инициализацию Firebase**:
   - Откройте `GhostApp.swift`
   - Убедитесь, что `FirebaseApp.configure()` вызывается в `init()`

3. **Проверьте подключение к интернету**:
   - Firebase Analytics требует интернет-соединение
   - События могут отправляться с задержкой

4. **Используйте DebugView**:
   - Добавьте аргумент `-FIRDebugEnabled` в Xcode
   - Запустите приложение
   - Откройте Firebase Console → Analytics → DebugView
   - События должны появляться в реальном времени

## 📝 Важные замечания

1. **Test Store от RevenueCat**: 
   - Покупки через Test Store будут отслеживаться как обычные покупки
   - Это нормально для тестирования аналитики

2. **Задержка данных**:
   - События в Firebase Console могут появляться с задержкой до 24 часов
   - Для тестирования используйте DebugView

3. **Первый запуск**:
   - Событие `first_open` отправляется только один раз на устройство
   - Для повторного тестирования удалите приложение и переустановите

4. **Триал vs Покупка**:
   - Система автоматически определяет, был ли это триал или покупка
   - Определение происходит через проверку `introductoryPrice` продукта

## 🔗 Полезные ссылки

- [Firebase Analytics Documentation](https://firebase.google.com/docs/analytics)
- [Firebase iOS Setup Guide](https://firebase.google.com/docs/ios/setup)
- [RevenueCat Test Store](https://www.revenuecat.com/docs/test-and-launch/test-store)
